version: '3.8'

services:
  # SQLite is embedded, no separate service needed
  # ChromaDB will use local persistent storage
  
  # Optional: Redis for caching (commented out for now)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

  # Optional: PostgreSQL for production (use instead of SQLite)
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: campus_ai
  #     POSTGRES_PASSWORD: campus_ai_password
  #     POSTGRES_DB: campus_ai_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

volumes:
  # redis_data:
  # postgres_data:
